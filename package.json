{
  "name": "discord-temp-voice-bot",
  "version": "1.0.0",
  "description": "Discord bot for temporary voice channels with premium features",
  "main": "src/bot.js",
  "scripts": {
    "start": "node src/bot.js",
    "dev": "nodemon src/bot.js",
    "check": "node src/bot.js --check-only",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dashboard": "cd src/web/server && npm start",
    "client": "cd src/web/client && npm start",
    "web": "start powershell -Command \"cd src/web/server && npm start\" && start powershell -Command \"cd src/web/client && npm start\""
  },
  "keywords": [
    "discord",
    "bot",
    "voice",
    "channels",
    "temporary"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@discordjs/builders": "^1.11.3",
    "axios": "^1.11.0",
    "discord.js": "^14.22.1",
    "dotenv": "^16.4.5",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
